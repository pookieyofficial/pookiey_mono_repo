version: "3.8"

services:
  web:
    image: ${WEB_IMAGE}
    container_name: pookiey-web
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
    ports:
      - "3000:3000"


